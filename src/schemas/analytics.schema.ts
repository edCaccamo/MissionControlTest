// All schemas are currently in media.schema.ts for simplicity.
// As the project grows, you'd split these into separate files.
export { yearSchema, topicSchema, dashboardSchema } from "./media.schema";
